FROM python:3.10-slim

# Install system dependencies for OpenCV (GUI dependencies are stripped, but core libraries are needed)
RUN apt-get update && apt-get install -y --no-install-recommends \
    libgl1 \
    libglib2.0-0 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Copy requirements and install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy the service code and face models
COPY face_service.py .
COPY models/ ./models/

EXPOSE 8000

ENV PORT=8000

# Start FastAPI server using uvicorn
CMD ["uvicorn", "face_service:app", "--host", "0.0.0.0", "--port", "8000"]
